{
  "name": "bfx-hf-ext-plugin-kraken",
  "version": "0.0.1",
  "description": "Kraken exchange plugin for the Honey Framework",
  "license": "Apache-2.0",
  "contributors": [
    "Cris Mihalache <me@f3rno.com>"
  ],
  "scripts": {
    "lint": "standard",
    "test": "npm run lint && npm run unit",
    "unit": "NODE_ENV=test istanbul cover _mocha -- -R spec -b --recursive",
    "test-without-coverage": "NODE_ENV=test mocha -R spec -b --recursive"
  },
  "dependencies": {
    "bluebird": "^3.5.5",
    "debug": "^4.1.1",
    "lodash": "^4.17.11",
    "node-kraken-api": "^0.4.1",
    "p-iteration": "^1.1.8"
  },
  "devDependencies": {
    "mocha": "^7.1.0",
    "standard": "^14.3.1"
  }
}
